doctype html
html 
  head 
    block head
      meta(charset="UTF-8")
      meta(name="viewport" content="width=device-width, initial-scale=1.0")
      link(rel="stylesheet" href="/css/style.css")
      link(rel="shortcut icon" type="image/png" href="/img/favicon.png")
      link(rel="stylesheet" href="https://fonts.googleapis.com/css?family=Lato:300,300i,700")
      //- 🖥️INTERPOLATION
      //- like template literal in ES6
      //- #{object}
      //-title Natours | #{tour}
      title Natours | #{title}

  body
    //HEADER
    include _header
    //CONTENT
    block content
        h1 This is a placeholder heading
    //- section.overview
    //-     h1=tour
    //FOOTER
    include _footer
    script(src="https://js.stripe.com/v3/")
    script(type="module" defer src="/js/bundled/index.js")
    //-script(defer src="../js/bundle.js")
    //-script(type = "module" defer src="https://cdnjs.cloudflare.com/ajax/libs/axios/1.1.3/axios.min.js")
    //- script(type = "module" defer src="../public/js/mapbox.js")
    //-script(type = "module" defer src="/js/login.js")

        //-🖥️BUFFERED CODE
            tour and user are defined as object properties in app.js
            h1= tour 
            h1 The Park Camper
            h2= user.toUpperCase()
            🖥️UNBUFFERED CODE
            not going to add anything to output
            - const x = 9
            h2= 2*x
            (h2 is 18)
            p This is just some text
